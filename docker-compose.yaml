services:
  # Backend API
  backend:
    build: ./server
    container_name: smart-restaurant-admin-backend
    ports:
      - "3001:3001"
    env_file:
      - ./server/.env
    networks:
      - smart-restaurant-admin-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Frontend (Nginx) - Reads .env for build
  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=${VITE_API_URL}
        - VITE_WS_URL=${VITE_WS_URL}
    container_name: smart-restaurant-admin-frontend
    env_file:
      - ./client/.env
    ports:
      - "5173:80"
    networks:
      - smart-restaurant-admin-network
    depends_on:
      - backend
    restart: unless-stopped

networks:
  smart-restaurant-admin-network:
    driver: bridge
